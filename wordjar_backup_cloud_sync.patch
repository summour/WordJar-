--- /mnt/data/index-5.html	2026-04-24 05:57:08.144689586 +0000
+++ /mnt/data/index_with_cloud_patch.html	2026-04-24 06:06:45.576575973 +0000
@@ -11,6 +11,10 @@
 <meta name="apple-mobile-web-app-title" content="WordJar">
 <meta name="mobile-web-app-capable" content="yes">
 <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
+<!-- Optional Firebase SDK for manual cloud backup sync. Fill FIREBASE_CONFIG in the script below to enable it. -->
+<script src="https://www.gstatic.com/firebasejs/10.12.5/firebase-app-compat.js"></script>
+<script src="https://www.gstatic.com/firebasejs/10.12.5/firebase-auth-compat.js"></script>
+<script src="https://www.gstatic.com/firebasejs/10.12.5/firebase-firestore-compat.js"></script>
 <style>
 :root{
   /* Minimalist Color Palette */
@@ -294,6 +298,10 @@
 .settings-card-title { font-size: 16px; font-weight: 700; color: var(--ink); margin-bottom: 16px; }
 .action-row { display: flex; gap: 10px; margin-bottom: 20px; }
 .format-label { font-size: 12px; color: var(--ink2); line-height: 1.5; background:var(--sur2); padding:10px; border-radius:10px;}
+.sync-status { font-size: 12px; color: var(--ink2); line-height: 1.5; background: var(--sur2); padding: 10px; border-radius: 10px; margin-bottom: 12px; }
+.sync-status.ok { background:#dcfce7; color:#166534; }
+.sync-status.warn { background:#fff7ed; color:#9a3412; }
+.sync-status.err { background:#fee2e2; color:#991b1b; }
 
 /* ── LEARNING SETTINGS MODAL ── */
 .opt-label { font-size: 14px; font-weight: 500; color: var(--ink); }
@@ -537,6 +545,24 @@
       <button class="btn btn-s" onclick="exportCSV(null)" style="padding: 10px 14px; font-size: 13px; flex:1;">Export CSV</button>
     </div>
     <div class="format-label"><span style="font-weight:700; color:var(--ink);">CSV Format:</span><br>Word, Type, Pronunciation, Meaning, Example, Notes</div>
+    <div class="settings-card-title" style="margin-top:20px;">JSON Backup</div>
+    <div class="action-row">
+      <button class="btn btn-s" onclick="exportJSONBackup()" style="padding: 10px 14px; font-size: 13px; flex:1;">Export JSON</button>
+      <input type="file" id="jsonInput" accept=".json,application/json" style="display:none" onchange="handleJSONImport(event)">
+      <button class="btn btn-s" onclick="document.getElementById('jsonInput').click()" style="padding: 10px 14px; font-size: 13px; flex:1;">Import JSON</button>
+    </div>
+    <div class="format-label"><span style="font-weight:700; color:var(--ink);">Local-first backup:</span><br>Exports the full WordJar data object including words, decks, profile, study history, and settings.</div>
+    <div class="settings-card-title" style="margin-top:20px;">Cloud Backup Sync</div>
+    <div id="cloudStatus" class="sync-status warn">Cloud sync is optional. Paste Firebase config in the code to enable sign in.</div>
+    <div class="action-row">
+      <button class="btn btn-s" onclick="signInCloud()" style="padding: 10px 14px; font-size: 13px; flex:1;">Sign In</button>
+      <button class="btn btn-s" onclick="signOutCloud()" style="padding: 10px 14px; font-size: 13px; flex:1;">Sign Out</button>
+    </div>
+    <div class="action-row">
+      <button class="btn btn-p" onclick="saveCloudBackup()" style="padding: 10px 14px; font-size: 13px; flex:1;">Save to Cloud</button>
+      <button class="btn btn-s" onclick="loadCloudBackup()" style="padding: 10px 14px; font-size: 13px; flex:1;">Load from Cloud</button>
+    </div>
+    <div class="format-label"><span style="font-weight:700; color:var(--ink);">Manual sync:</span><br>Use Save to Cloud on this device, then Load from Cloud on another device. This avoids accidental overwrites.</div>
   </div>
   <button class="btn btn-s btn-full" style="color:var(--anki-again); border-color:#fca5a5; background:#fef2f2; margin: 0 20px; width: calc(100% - 40px); padding: 14px; font-size: 14px;" onclick="clearAll()">Clear All Data</button>
 </div>
@@ -772,6 +798,15 @@
 let currentStudyDeckId = null;
 let importTargetDeckId = null; 
 
+// Optional Firebase cloud backup sync. Replace placeholders with your Firebase web app config.
+const FIREBASE_CONFIG = {
+  apiKey: "PASTE_API_KEY_HERE",
+  authDomain: "PASTE_PROJECT_ID.firebaseapp.com",
+  projectId: "PASTE_PROJECT_ID",
+  appId: "PASTE_APP_ID_HERE"
+};
+let cloudAuth = null, cloudDb = null, cloudUser = null;
+
 // Multi-select State
 let isSelectMode = false;
 let selectedCards = new Set();
@@ -1388,6 +1423,148 @@
   } catch(err) { toast('Import failed'); } document.getElementById('csvInput').value='';
 }
 
+// ══════════════════════════════════════════════
+// JSON BACKUP + OPTIONAL FIREBASE CLOUD SYNC
+// ══════════════════════════════════════════════
+function normalizeWordJarData(data) {
+  if (!data || typeof data !== 'object') throw new Error('Invalid backup');
+  data.words = Array.isArray(data.words) ? data.words : [];
+  data.decks = Array.isArray(data.decks) && data.decks.length ? data.decks : [{ id: 'd1', name: 'Default', desc: '', color: '#09090b' }];
+  data.profile = data.profile || { name: 'User' };
+  data.studyDays = data.studyDays || {};
+  data.todayDone = Number(data.todayDone || 0);
+  data.lastDate = data.lastDate || new Date().toDateString();
+  return data;
+}
+
+function exportJSONBackup() {
+  const payload = {
+    app: 'WordJar',
+    version: SK,
+    exportedAt: new Date().toISOString(),
+    data: D
+  };
+  const json = JSON.stringify(payload, null, 2);
+  const date = new Date().toISOString().slice(0, 10);
+  const a = document.createElement('a');
+  a.href = URL.createObjectURL(new Blob([json], { type: 'application/json;charset=utf-8' }));
+  a.download = `wordjar-backup-${date}.json`;
+  a.click();
+  URL.revokeObjectURL(a.href);
+  toast('JSON backup exported');
+}
+
+function handleJSONImport(event) {
+  const file = event.target.files[0];
+  if (!file) return;
+  const reader = new FileReader();
+  reader.onload = e => {
+    try {
+      const parsed = JSON.parse(e.target.result);
+      const restored = normalizeWordJarData(parsed.data || parsed);
+      if (!Array.isArray(restored.words) || !Array.isArray(restored.decks)) throw new Error('Invalid structure');
+      if (confirm('Restore this JSON backup? This will replace current local WordJar data.')) {
+        D = restored;
+        save();
+        updateHome(); renderWords(); renderDecks(); updateAccount();
+        toast('JSON backup restored');
+      }
+    } catch (err) {
+      alert('Invalid WordJar JSON backup. No data was changed.');
+    }
+    event.target.value = '';
+  };
+  reader.readAsText(file, 'UTF-8');
+}
+
+function firebaseConfigReady() {
+  return FIREBASE_CONFIG && FIREBASE_CONFIG.apiKey && !FIREBASE_CONFIG.apiKey.includes('PASTE_') && FIREBASE_CONFIG.projectId && !FIREBASE_CONFIG.projectId.includes('PASTE_');
+}
+
+function setCloudStatus(message, mode = 'warn') {
+  const el = document.getElementById('cloudStatus');
+  if (!el) return;
+  el.textContent = message;
+  el.className = `sync-status ${mode}`;
+}
+
+function initCloudSync() {
+  if (!firebaseConfigReady()) {
+    setCloudStatus('Cloud sync is not configured yet. JSON backup works without setup.', 'warn');
+    return;
+  }
+  if (!window.firebase) {
+    setCloudStatus('Firebase SDK failed to load.', 'err');
+    return;
+  }
+  try {
+    if (!firebase.apps.length) firebase.initializeApp(FIREBASE_CONFIG);
+    cloudAuth = firebase.auth();
+    cloudDb = firebase.firestore();
+    cloudAuth.onAuthStateChanged(user => {
+      cloudUser = user;
+      if (user) setCloudStatus(`Signed in: ${user.email || user.uid}`, 'ok');
+      else setCloudStatus('Cloud is configured. Sign in to save or load backups.', 'warn');
+    });
+  } catch (err) {
+    setCloudStatus('Cloud setup failed. Check Firebase config and allowed domains.', 'err');
+  }
+}
+
+async function signInCloud() {
+  if (!cloudAuth) return toast('Paste Firebase config first');
+  try {
+    await cloudAuth.signInWithPopup(new firebase.auth.GoogleAuthProvider());
+  } catch (err) {
+    toast('Sign in failed');
+  }
+}
+
+async function signOutCloud() {
+  if (!cloudAuth) return toast('Cloud is not configured');
+  await cloudAuth.signOut();
+  toast('Signed out');
+}
+
+function cloudBackupRef() {
+  if (!cloudDb || !cloudUser) return null;
+  return cloudDb.collection('wordjarUsers').doc(cloudUser.uid).collection('backups').doc('main');
+}
+
+async function saveCloudBackup() {
+  const ref = cloudBackupRef();
+  if (!ref) return toast('Sign in first');
+  try {
+    await ref.set({
+      app: 'WordJar',
+      version: SK,
+      updatedAt: firebase.firestore.FieldValue.serverTimestamp(),
+      data: D
+    });
+    toast('Saved to cloud');
+  } catch (err) {
+    toast('Cloud save failed');
+  }
+}
+
+async function loadCloudBackup() {
+  const ref = cloudBackupRef();
+  if (!ref) return toast('Sign in first');
+  try {
+    const snap = await ref.get();
+    if (!snap.exists) return toast('No cloud backup found');
+    const restored = normalizeWordJarData(snap.data().data);
+    if (confirm('Load cloud backup and replace current local data?')) {
+      D = restored;
+      save();
+      updateHome(); renderWords(); renderDecks(); updateAccount();
+      toast('Loaded from cloud');
+    }
+  } catch (err) {
+    toast('Cloud load failed');
+  }
+}
+
 // ── OVERLAY LISTENER ──
 document.querySelectorAll('.overlay').forEach(m => {
   m.addEventListener('mousedown', e => { if(e.target === m) closeO(m.id); }); 
@@ -1400,7 +1577,7 @@
   }, 1500); // แสดงหน้าโหลด 1.5 วินาที
 });
 
-load(); seedIfEmpty(); updateHome();
+load(); seedIfEmpty(); updateHome(); initCloudSync();
 </script>
 </body>
 </html>
